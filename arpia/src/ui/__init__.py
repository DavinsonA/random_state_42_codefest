"""Las interfaces: `static/`.

El chat (Reto 1) y el tablero (Reto 2), en HTML, CSS y JavaScript plano,
servidos por el mismo contenedor de la API (`src/api/routing.py`, que decide
cual entregar en `/` segun la cabecera `Host`).

Sin framework, sin build y sin ninguna referencia externa: si la red del venue
falla durante la evaluacion, la interfaz sigue en pie. Las librerias van
vendorizadas en `static/vendor/`.
"""
